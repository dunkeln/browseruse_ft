[project]
name = "browseruse-ft"
version = "0.1.0"
description = "Browser Use Cloud tools for a hosted Fireworks RFT evaluator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "browser-use-sdk==3.10.0",
    "eval-protocol==0.3.32",
    "playwright==1.62.0",
    "python-dotenv==1.2.2",
]

[tool.uv]
package = false
